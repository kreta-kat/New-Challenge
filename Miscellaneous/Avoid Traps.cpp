#include <bits/stdc++.h> 
int avoidTraps(vector<int>& obstacles, int n) 
{
    // Write your code here.
    int no= INT_MAX;
    sort(obstacles.begin(), obstacles.end());
    for(int i=obstacles[0]; i<n; i++)
    {
         if(obstacles[i] != i)
         {
             
         }

         
    }
    return no;

}
